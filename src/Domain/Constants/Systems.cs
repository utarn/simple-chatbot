namespace ChatbotApi.Domain.Constants;

/// <summary>
/// Centralized constant values for shared system constants.
/// Plugin name string constants were removed per request; only PageSize remains.
/// </summary>
public abstract class Systems
{
    // General
    public const int PageSize = 20;
}
